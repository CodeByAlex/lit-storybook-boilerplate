import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';
import { html } from 'lit';
import './button.ts';

<Meta
  title="Components / Button"
  component="example-button"
  argTypes={{
    buttonText: 'Button',
    variant: {
      options: ['default', 'primary', 'success', 'info', 'warning', 'danger'],
      control: { type: 'select' }
    },
  }}
/>

# Beacon Button

Stories that show the different variations of a button.

<Canvas withSource="open">
  <Story
    name="Button - default"
    component="example-button"
    args={{
      buttonText: 'Default Button',
      variant: 'default',
    }}
  >
    {(args) => html`
        <example-button variant="${args.variant}">${args.buttonText}</example-button>
    `}
  </Story>
</Canvas>

<ArgsTable story="Button - default" />

<Canvas withSource="open">
  <Story
    name="Button - primary"
    component="example-button"
    args={{
      buttonText: 'Primary Button',
      variant: 'primary',
    }}
  >
    {(args) => html`
        <example-button variant="${args.variant}">${args.buttonText}</example-button>
    `}
  </Story>
</Canvas>

<ArgsTable story="Button - primary" />

<Canvas withSource="open">
  <Story
    name="Button - success"
    component="example-button"
    args={{
      buttonText: 'Success Button',
      variant: 'success',
    }}
  >
    {(args) => html`
        <example-button variant="${args.variant}" >${args.buttonText}</example-button>
    `}
  </Story>
</Canvas>

<ArgsTable story="Button - sucsess" />

<Canvas withSource="open">
  <Story
    name="Button - info"
    component="example-button"
    args={{
      buttonText: 'Info Button',
      variant: 'info',
    }}
  >
    {(args) => html`
        <example-button variant="${args.variant}" >${args.buttonText}</example-button>
    `}
  </Story>
</Canvas>

<ArgsTable story="Button - info" />

<Canvas withSource="open">
  <Story
    name="Button - warning"
    component="example-button"
    args={{
      buttonText: 'Warning Button',
      variant: 'warning',
    }}
  >
    {(args) => html`
        <example-button variant="${args.variant}" >${args.buttonText}</example-button>
    `}
  </Story>
</Canvas>

<ArgsTable story="Button - warning" />

<Canvas withSource="open">
  <Story
    name="Button - danger"
    component="example-button"
    args={{
      buttonText: 'Danger Button',
      variant: 'danger',
    }}
  >
    {(args) => html`
        <example-button variant="${args.variant}" >${args.buttonText}</example-button>
    `}
  </Story>
</Canvas>

<ArgsTable story="Button - danger" />

